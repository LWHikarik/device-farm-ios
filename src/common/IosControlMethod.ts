// String values are the wire protocol with the browser — do not rename them.
export enum IosControlMethod {
    CLICK = 'CLICK',
    SCROLL = 'SCROLL',
    LONG_PRESS = 'LONG_PRESS',
    PRESS_BUTTON = 'PRESS_BUTTON',
    GET_SCREEN_WIDTH = 'GET_SCREEN_WIDTH',
    APPIUM_SETTINGS = 'APPIUM_SETTINGS',
    SEND_KEYS = 'SEND_KEYS',
}
